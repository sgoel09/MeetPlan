package com.example.meetplan.browse.addtask;

/** Interface that dismisses a fragment if implemented. */
public interface FragmentDismisser {

    /** Method to dismiss the fragment. */
    void dismissFragment();
}
